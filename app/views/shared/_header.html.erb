<header>
  <nav class="navbar navbar-expand-lg navigation navbar-light bg-light">
    <div class="container">
      <%= link_to(stubs_path, class: 'navbar-brand') do %>
        <%= image_tag('stubsnaplogo.jpg', alt: 'Stub Snap Logo') %>
      <% end %>
      <button class='navbar-toggler' data-bs-toggle='collapse' data-bs-target='#navbarSupportedContent'
        aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation'>
        <span class='navbar-toggler-icon'></span>
      </button>

      <div class='collapse navbar-collapse' id='navbarSupportedContent'>
        <ul class='navbar-nav ms-auto main-nav align-items-center'>
          <%= link_to new_stub_path do %>
            <i class="bi bi-plus-circle add-stub-icon">add stub</i>
          <% end %>
          <li class="nav-item dropdown">
            <a class="nav-link" type="button" data-bs-toggle="dropdown" aria-expanded="false">
              <h1 class="bi bi-person-circle"></h1>
            </a>
            <div class="dropdown-menu dropdown-menu-left">
              <div class="dropdown-item"><%= current_user.name %></div>
              <div class="dropdown-divider"></div>
              <%= link_to 'My Page', my_page_stubs_path, class: 'dropdown-item' %>
              <%= link_to (t 'profiles.show.title'), profile_path, class: 'dropdown-item' %>
              <%= link_to (t 'stubs.my_liking.title'), my_liking_stubs_path, class: 'dropdown-item' %>
              <%= link_to (t 'defaults.logout'), logout_path, class: 'dropdown-item', data: { "turbo-method": :delete } %>
            </div>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>