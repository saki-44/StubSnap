<header>
  <nav class="navbar navbar-expand-lg custom-bg-color">
    <div class="container">
      <%= link_to(stubs_path, class: 'navbar-brand') do %>
        <%= image_tag('stubsnaplogo.png', alt: 'Stub Snap Logo', size: "60x60") %>
      <% end %>
      <button class='navbar-toggler' data-bs-toggle='collapse' data-bs-target='#navbarSupportedContent'
        aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation'>
        <span class='navbar-toggler-icon'></span>
      </button>

      <div class='collapse navbar-collapse' id='navbarSupportedContent'>
        <ul class='navbar-nav ms-auto main-nav align-items-center'>
          <li class="nav-item spacing-right">
            <%= link_to new_stub_path, class: "btn btn-danger" do %>
              + Stub 登録
            <% end %>
          </li>
          <li class="nav-item dropdown">
            <button class="nav-link" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="bi bi-person-circle"></i>
            </button>
            <ul class="dropdown-menu dropdown-menu-end">
              <li class="dropdown-item"><%= current_user.name %></li>
              <li><hr class="dropdown-divider"></li>
              <%= link_to (t 'profiles.show.title'), profile_path, class: 'dropdown-item' %>
              <%= link_to (t 'stubs.my_page.title'), my_page_stubs_path, class: 'dropdown-item' %>
              <%= link_to (t 'stubs.my_liking.title'), my_liking_stubs_path, class: 'dropdown-item' %>
              <%= link_to (t 'defaults.logout'), logout_path, class: 'dropdown-item', data: { turbo_method: :delete } %>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>